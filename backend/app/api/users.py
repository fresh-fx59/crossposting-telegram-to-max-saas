"""User management API routes."""

from fastapi import APIRouter

router = APIRouter()

# User settings endpoints removed — Max credentials are now managed
# per Max channel via /api/connections/max endpoints.
